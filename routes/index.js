const debug = require('debug')('run__on::server::routes/index.js')

/**
 * Configures Express.Router
 *
 * @param {Object} app - Express application
 * @returns {undefined}
 */
module.exports = (app) => {
  debug('configuring express application router')
}
